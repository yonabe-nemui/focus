# Focus Project Memory

このファイルは、プロジェクトの開発過程で得られた知見、主要な機能、および固有の設定を保持するためのものです。

## アプリの概要と機能
- **目的:** Googleニュース、はてなブックマーク、BlueSkyの情報を一括でチェックできる情報集約アプリ。
- **コアバリュー:** 「見たくないものは見ない」。ユーザーが情報を完全に制御し、不要な情報を徹底的に排除できる環境を提供すること。
- **主要機能:**
    - **Googleニュース:** 複数カテゴリの統合表示。
    - **はてなブックマーク:** 人気・新着・ITカテゴリの統合表示。ブックマーク数の表示。
    - **BlueSky:** 
        - ログイン（2段階認証対応）およびセッションの永続化。
        - **クライアントサイド・フィルタリング:** BlueSky側で設定した「ミュート済みワード」を同期し、検索結果から該当する投稿をアプリ側で自動除外する。
        - 検索結果の統合表示（デフォルトキーワード: IT）。
        - パスワード可視化トグル、OSのオートフィル（自動入力）対応。

## 技術スタック
- **言語:** Kotlin (Kotlin Multiplatform)
- **UI:** Compose Multiplatform (Material 3)
- **ネットワーク:** Ktor (JSON/XML対応)
- **データベース:** SQLDelight (セッション情報の永続化)
- **シリアライズ:** kotlinx-serialization (JSON), xmlutil (XML/RDF)
- **日付処理:** kotlinx-datetime

## プロジェクト固有のルール・設定
### インポートの方針
- **スターインポートの禁止:** `import package.*` による省略は避け、すべてのクラスや関数を個別に明示的にインポートする。これはIDEの参照解決を確実にし、名前の競合を防ぐため。(`SKILL.md` に記載済み)

### データベース管理 (SQLDelight)
- **型変換の簡素化:** Boolean値を扱う際は、SQLDelightのAdapterエラーを避けるため、`INTEGER` (0/1) を `Long` 型として扱う構成を推奨。
- **スキーマ変更:** テーブル定義（`.sq`ファイル）を変更した後は、既存のアプリを一度アンインストールしてデータベースを再作成する必要がある（開発フェーズ）。本番運用時は `.sqm` によるマイグレーションが必要。

### ネットワーク設定 (Ktor)
- **ContentNegotiation:** JSONを扱う全プラットフォームのエントリーポイントで `install(ContentNegotiation)` と `json()` の設定が必須。
- **User-Agent:** はてなブックマーク等のRSS取得時は、将来的に適切なUser-Agentの設定が推奨される。

### マルチモジュール構成
- **依存関係の露出:** `shared` モジュールで `implementation` されたライブラリは `composeApp` から直接参照できない。`HttpClient` の設定などで `composeApp` 側でもライブラリが必要な場合は、両方の `build.gradle.kts` に記述するか、`shared` 側で `api` を使用する。

## 実装上の注意点
- **BlueSkyの仕様:** 
    - ログインしていても、検索API (`searchPosts`) の結果にはサーバー側でミュートワードが適用されない。必ずクライアント側での `filter` 処理が必要。
    - 2段階認証 (2FA) が有効なアカウントでは、最初のログイン試行で 401 `AuthFactorTokenRequired` が返る。
